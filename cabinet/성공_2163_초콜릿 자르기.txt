fun main(args: Array<String>) {
    val split = readLine()?.split(" ") ?: arrayListOf("2", "2")
    val N = split[0].toInt()
    val M = split[1].toInt()
    var count = (M - 1) + M.times(N - 1)
    
    println("${count}")
}
